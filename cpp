#include <iostream>
using namespace std;

int main(){
  int numUsers = 0;
  int updateDirection = 0;
  
  numUsers = 8;
  updateDirection = 1;
  
  numUsers = (updatedDirection == 1)?(numUsers +1) : (numUsers -1);
  
  cout <<"New value is:"<<numUsers<< endl;
  
  return 0;
  }
